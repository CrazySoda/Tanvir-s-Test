name: Test Runner on Raspberry Pi

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: [self-hosted, raspberrypi]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run echo command
        run: echo "Hello from Raspberry Pi Runner"
